import Opening0 from "@/components/Opening0/Opening0";

/** Temporary preview route. opening0 is not wired into the Opening sequence
 * yet — the transition into opening1 is a later iteration. */
export default function Opening0Page() {
  return (
    <main className="min-h-screen bg-ink">
      <Opening0 />
    </main>
  );
}
