/** Which timeline view the Mode switcher has selected. `top10` is an
 * alternative view of the default generation timeline, not a sort mode — it
 * leaves the sort selection untouched. */
export type ViewMode = "default" | "top10" | "explore";
